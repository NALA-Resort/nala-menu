node_modules/
package-lock.json
# the print suite's rendered artifacts, one pattern rather than a name
# per file: _p2_dietpdf.pdf arrived unlisted and sat untracked until a
# hook complained about it mid-run
_p*
__pycache__/
