node_modules/
package-lock.json
# the print suites' rendered artifacts, one pattern rather than a name per
# file: _p2_dietpdf.pdf arrived unlisted and BOTH sessions tripped over it
# the same day, each fixing it their own way. The pattern ends the race.
_p*
__pycache__/
